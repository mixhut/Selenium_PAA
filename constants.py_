from main import PROJECT_NAME

GECKOPATH = r'/path/to/geckodriver'
FF_PROFILE = r'/path/to/.mozilla/firefox/profile'

PROJECT_DIR = r'/path/to/selenium_paa'

CSV_FILE = PROJECT_DIR + '/output/PAA_Output.csv'
JSON_FILE = PROJECT_DIR + '/output/PAA_Output.json'
PICKLE_FILE = PROJECT_DIR + '/output/' + PROJECT_NAME + '.pickle'
CHECKPOINT_FILE = PROJECT_DIR + '/checkpoints/' + PROJECT_NAME + '-progress.txt'

URL = 'https://www.google.com/search?hl=en'